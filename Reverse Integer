class Solution(object):
    def reverse(self, x):
        """
        :type x: int
        :rtype: int
        """
        y = str(abs(x))
        y = int(y[::-1])
        if x < 0:
            y *= -1
        if y > 2**31-1 or y < -2**31:
            return 0
        return y
      
        
